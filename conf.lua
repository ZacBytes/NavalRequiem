function love.conf(t)
    t.window.title = "Naval Requiem"         -- The window title (string)
    t.window.icon = "Art/Icon.bmp"                 -- Filepath to an image to use as the window's icon (string)
end